При выполнении этого задания я создала коллекцию, создала окружение,
параметризировала переменные, которые сохранены в переменных окружения.

Написала Pre-Request Script для передачи обязательного ContentType в заголовках запросов.

Получала токен авторизации и передавала вместе с запросом заголовок Authorization: Token {{token}}.

Осуществляла парсинг идентификаторов из тела ответа от сервера на вкладке Post-Response Scripts
в коллекцию переменных для переиспользования этих идентификаторов с другими запросами.

Находила несоответствия в документации, анализировала тело ответа и самостоятельно искала варианты исправления,
вносила изменения в тело запросов для корректной отработки их сервером.